# Nuxt
.nuxt
.output
.data
dist

# Node
node_modules
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage / test output
coverage
.nitro
.vercel
.netlify

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example

# Editor — VS Code user state
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example

# Editor — JetBrains user state lives in .idea/.gitignore
# (shared project config in .idea/ is committed)

# Editor — Claude Code session state
.claude/

# Icon generator font cache (re-fetched on demand)
scripts/.cache
